import {
    // create naive ui
    create,
    // component
    NButton,
    NWatermark,
    NSpace,
    NSkeleton,
    NImage,
    NPagination,
} from 'naive-ui'
const naive = create({
    components: [
        NButton,
        NWatermark,
        NSpace,
        NSkeleton,
        NImage,
        NPagination,
    ]
})

export default naive