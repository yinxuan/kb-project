import {
    // create naive ui
    create,
    // component
    NButton,
    NWatermark,
    NSpace,
    NSkeleton,
    NImage,
} from 'naive-ui'
const naive = create({
    components: [
        NButton,
        NWatermark,
        NSpace,
        NSkeleton,
        NImage,
    ]
})

export default naive